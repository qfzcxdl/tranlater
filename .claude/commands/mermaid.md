mermaid可视化这个模块
1、组件层级说明
2、用mermaid绘制类的继承依赖图
3、用mermaid绘制核心逻辑流程图
4、用mermaid绘制数据流程图
5、如果可以输出时序图的话请用mermaid绘制时序图

将以上的写到个对应的五个.mmd 文件，生成文件按照{序号}_{模块名}_{图名}.mmd命名,例如:01_check_seed_class.mmd
Mermaid版本要求在10.3.0,不要用```mermaid开头

生成mermaid文件遵循如下规则
1、如果存在层级结构尽量用不同颜色的区分开
2、尽量用中文解释描述内容
3、将生成的mmd文件存储在模块下的README文件夹下。
